#include<bits/stdc++.h>

using namespace std;

void partition(vector<int>& arr, int low,int mid , int high) {

}


/*
 * Quick Sort Logic:
*/
void quick_sort(vector<int>& arr, int low, int high) {
}

int main() {
    vector<int> v = {4,2,1,6,7};
    quick_sort(v, 0, v.size()-1);

    for (int i: v) {
        cout << i << ", ";
    }
    cout << endl;
    return 0;
}
